# Prisma reads DATABASE_URL via schema.prisma's `env("DATABASE_URL")`.
# Override in prod with the managed Postgres DSN.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/notes
SESSION_SECRET=dev-only-notes-secret-change-me
